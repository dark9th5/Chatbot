version: '3.8'
services:
  web-admin:
    build: .
    image: news-chatbot-admin:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
